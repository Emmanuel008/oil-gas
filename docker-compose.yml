# Run: ./scripts/fix-port-and-start.sh  (or docker compose up -d --build)
# App: http://localhost:2020
services:
  frontend:
    build: .
    container_name: oil-gas-frontend
    ports:
      - "2020:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
